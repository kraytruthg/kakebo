services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: kakebo
      POSTGRES_PASSWORD: kakebo
      POSTGRES_DB: kakebo_development
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
