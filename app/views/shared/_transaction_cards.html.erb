<%# locals: (transactions:, show_account_name: true, empty_message: nil) %>
<div class="lg:hidden space-y-2">
  <% if transactions.empty? %>
    <div class="bg-white rounded-xl border border-slate-100 py-12 text-center text-slate-400">
      <p class="text-sm"><%= empty_message || "沒有符合條件的交易紀錄" %></p>
    </div>
  <% else %>
    <% transactions.each do |t| %>
      <div class="<%= t.transfer? ? 'bg-purple-50/50 border-purple-100' : 'bg-white border-slate-100' %> rounded-xl border px-4 py-3"
           id="transaction-mobile-<%= t.id %>">
        <div class="flex items-start justify-between">
          <div class="flex-1 min-w-0">
            <div class="flex items-center gap-2">
              <% if t.reconciled? %>
                <%= icon "lock-closed", classes: "w-3 h-3 text-slate-300" %>
              <% elsif t.cleared? %>
                <span class="inline-block w-3 h-3 rounded-full border-2 border-indigo-400 bg-indigo-100"></span>
              <% else %>
                <span class="inline-block w-3 h-3 rounded-full border-2 border-slate-200"></span>
              <% end %>
              <span class="text-xs text-slate-400"><%= t.date.strftime("%m/%d") %></span>
              <span class="text-sm text-slate-800 truncate"><%= t.memo.presence || "─" %></span>
            </div>
            <p class="text-xs text-slate-400 mt-0.5">
              <%= transaction_category_label(t) %><%= " · #{t.account.name}" if show_account_name && !t.transfer? %>
            </p>
          </div>
          <span class="text-sm font-semibold ml-3 <%= t.amount < 0 ? 'text-red-500' : 'text-emerald-600' %>">
            <%= format_amount(t.amount) %>
          </span>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
