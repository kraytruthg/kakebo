# 帳戶間轉帳（Transfer）實作計畫

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在兩個帳戶之間建立轉帳，自動產生兩筆互相關聯的交易，刪除時連動刪除，不影響預算類別。

**Architecture:** 使用已存在的 `transfer_pair_id` 欄位做 self-referential 關聯；獨立 `TransfersController`（`/transfers`，not nested under account）負責 new / create / destroy；transaction row 依 `transfer?` 切換顯示與刪除路徑。

**Tech Stack:** Rails 8.1, Hotwire（無 Turbo Stream，redirect only），Tailwind CSS v4，RSpec + Capybara system spec

---

## 前置確認

```bash
# 確認 transfer_pair_id 欄位存在
grep "transfer_pair_id" db/schema.rb
# 應看到：t.integer "transfer_pair_id"
```

---

### Task 1: Transaction model — 加入 self-referential association

**Files:**
- Modify: `app/models/transaction.rb`
- Test: `spec/models/transaction_spec.rb`（建立新檔）

**Step 1: 建立 model spec 並寫失敗測試**

建立 `spec/models/transaction_spec.rb`：

```ruby
require "rails_helper"

RSpec.describe Transaction, type: :model do
  let(:household) { create(:household) }
  let(:account_a) { create(:account, household: household) }
  let(:account_b) { create(:account, household: household) }

  describe "#transfer?" do
    it "transfer_pair_id 為 nil 時回傳 false" do
      txn = create(:transaction, account: account_a, transfer_pair_id: nil)
      expect(txn.transfer?).to eq(false)
    end

    it "transfer_pair_id 存在時回傳 true" do
      outgoing = create(:transaction, account: account_a, category: nil, transfer_pair_id: nil)
      incoming = create(:transaction, account: account_b, category: nil, transfer_pair_id: outgoing.id)
      outgoing.update!(transfer_pair_id: incoming.id)
      expect(outgoing.reload.transfer?).to eq(true)
    end
  end

  describe "#income?" do
    it "category 有值時回傳 false" do
      txn = create(:transaction, account: account_a)
      expect(txn.income?).to eq(false)
    end

    it "category 為 nil 且非轉帳時回傳 true" do
      txn = create(:transaction, account: account_a, category: nil, transfer_pair_id: nil)
      expect(txn.income?).to eq(true)
    end

    it "轉帳交易回傳 false（即使 category 為 nil）" do
      outgoing = create(:transaction, account: account_a, category: nil, transfer_pair_id: nil)
      incoming = create(:transaction, account: account_b, category: nil, transfer_pair_id: outgoing.id)
      outgoing.update!(transfer_pair_id: incoming.id)
      expect(outgoing.reload.income?).to eq(false)
    end
  end

  describe "#transfer_pair" do
    it "回傳互相關聯的另一筆交易" do
      outgoing = create(:transaction, account: account_a, category: nil, transfer_pair_id: nil)
      incoming = create(:transaction, account: account_b, category: nil, transfer_pair_id: outgoing.id)
      outgoing.update!(transfer_pair_id: incoming.id)
      expect(outgoing.reload.transfer_pair).to eq(incoming)
      expect(incoming.reload.transfer_pair).to eq(outgoing)
    end
  end
end
```

注意：`create(:transaction, ...)` factory 預設有 category，要傳 `category: nil` 才能建收入/轉帳類型。

**Step 2: 執行測試確認失敗**

```bash
bundle exec rspec spec/models/transaction_spec.rb
```

預期：`transfer_pair` 相關測試失敗，因為 association 還沒定義。

**Step 3: 加入 self-referential association 到 Transaction model**

在 `app/models/transaction.rb` 的 `belongs_to :category` 後面加入：

```ruby
belongs_to :transfer_pair, class_name: "Transaction", foreign_key: :transfer_pair_id, optional: true
```

**Step 4: 再次執行測試確認通過**

```bash
bundle exec rspec spec/models/transaction_spec.rb
```

預期：3 個 describe 共 5 examples，0 failures。

**Step 5: Commit**

```bash
git add app/models/transaction.rb spec/models/transaction_spec.rb
git commit -m "feat: add transfer_pair association to Transaction model"
```

---

### Task 2: 加入路由

**Files:**
- Modify: `config/routes.rb`

**Step 1: 在 routes.rb 加入**

在 `resources :accounts` 區塊後、`get "budget/..."` 前加入：

```ruby
resources :transfers, only: [:new, :create, :destroy]
```

**Step 2: 確認路由正確產生**

```bash
bundle exec rails routes | grep transfer
```

預期看到：
```
new_transfer  GET   /transfers/new(.:format)   transfers#new
transfers     POST  /transfers(.:format)        transfers#create
transfer      DELETE /transfers/:id(.:format)  transfers#destroy
```

**Step 3: Commit**

```bash
git add config/routes.rb
git commit -m "feat: add transfers routes"
```

---

### Task 3: TransfersController#new 與表單頁

**Files:**
- Create: `app/controllers/transfers_controller.rb`
- Create: `app/views/transfers/new.html.erb`

**Step 1: 建立 controller**

```ruby
# app/controllers/transfers_controller.rb
class TransfersController < ApplicationController
  def new
    @accounts = Current.household.accounts.active.order(:name)
    @from_account_id = params[:from_account_id]
  end

  def create
    # Task 4 實作
  end

  def destroy
    # Task 6 實作
  end
end
```

**Step 2: 建立表單 view**

```erb
<%# app/views/transfers/new.html.erb %>
<div class="max-w-lg mx-auto px-4 sm:px-6 py-8">
  <div class="mb-6">
    <%= link_to accounts_path, class: "inline-flex items-center gap-1 text-sm text-slate-400 hover:text-slate-700" do %>
      <%= icon "chevron-left", classes: "w-4 h-4" %>
      帳戶
    <% end %>
    <h1 class="text-2xl font-bold text-slate-900 mt-2">帳戶間轉帳</h1>
  </div>

  <% if @error %>
    <div class="mb-4 rounded-lg bg-red-50 border border-red-200 px-4 py-3 text-sm text-red-700">
      <%= @error %>
    </div>
  <% end %>

  <div class="bg-white rounded-2xl border border-slate-100 shadow-sm p-6">
    <%= form_with url: transfers_path do |f| %>
      <div class="space-y-5">
        <div>
          <label class="block text-sm font-medium text-slate-700 mb-1.5">來源帳戶</label>
          <%= f.select :from_account_id,
                options_for_select(@accounts.map { |a| [a.name, a.id] }, @from_account_id),
                { prompt: "選擇來源帳戶" },
                class: "block w-full rounded-lg border border-slate-300 px-3.5 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
        </div>
        <div>
          <label class="block text-sm font-medium text-slate-700 mb-1.5">目標帳戶</label>
          <%= f.select :to_account_id,
                options_for_select(@accounts.map { |a| [a.name, a.id] }),
                { prompt: "選擇目標帳戶" },
                class: "block w-full rounded-lg border border-slate-300 px-3.5 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
        </div>
        <div>
          <label class="block text-sm font-medium text-slate-700 mb-1.5">金額</label>
          <%= f.number_field :amount, step: 1, min: 1, placeholder: "請填正整數，例：1000",
                class: "block w-full rounded-lg border border-slate-300 px-3.5 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
        </div>
        <div>
          <label class="block text-sm font-medium text-slate-700 mb-1.5">日期</label>
          <%= f.date_field :date, value: Date.today,
                class: "block w-full rounded-lg border border-slate-300 px-3.5 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
        </div>
        <div>
          <label class="block text-sm font-medium text-slate-700 mb-1.5">備註（選填）</label>
          <%= f.text_field :memo, placeholder: "例：存款",
                class: "block w-full rounded-lg border border-slate-300 px-3.5 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
        </div>
        <div class="flex gap-3 pt-2">
          <%= link_to "取消", accounts_path,
                class: "flex-1 text-center py-2.5 rounded-lg border border-slate-300 text-sm text-slate-600 hover:bg-slate-50 transition-colors" %>
          <%= f.submit "確認轉帳",
                class: "flex-1 bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-2.5 rounded-lg cursor-pointer transition-colors text-sm" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
```

**Step 3: 手動驗證頁面可存取**

```bash
bundle exec rails server
# 瀏覽 http://localhost:3000/transfers/new
# 應看到轉帳表單頁，無 error
```

**Step 4: Commit**

```bash
git add app/controllers/transfers_controller.rb app/views/transfers/new.html.erb
git commit -m "feat: add TransfersController#new and transfer form view"
```

---

### Task 4: 帳戶頁加入「轉帳」按鈕

**Files:**
- Modify: `app/views/accounts/show.html.erb`

**Step 1: 在按鈕區加入「轉帳」按鈕**

找到 `app/views/accounts/show.html.erb` 的按鈕區（`flex gap-2 mt-3 justify-end` 那一段），在「編輯」按鈕後、「新增交易」按鈕前插入：

```erb
<%= link_to new_transfer_path(from_account_id: @account.id),
      class: "inline-flex items-center gap-1.5 text-xs text-slate-500 hover:text-slate-900 border border-slate-200 rounded-lg px-3 py-1.5 transition-colors" do %>
  <%= icon "arrows-right-left", classes: "w-3.5 h-3.5" %>
  轉帳
<% end %>
```

**Step 2: 確認按鈕顯示**

```bash
bundle exec rails server
# 瀏覽任一帳戶頁，應看到 [編輯] [轉帳] [新增交易] 三個按鈕
# 點「轉帳」應跳到 /transfers/new?from_account_id=xxx，且來源帳戶預選當前帳戶
```

**Step 3: Commit**

```bash
git add app/views/accounts/show.html.erb
git commit -m "feat: add transfer button to account show page"
```

---

### Task 5: TransfersController#create 與 system spec（happy path）

**Files:**
- Modify: `app/controllers/transfers_controller.rb`
- Create: `spec/system/transfers_spec.rb`

**Step 1: 先寫失敗的 system spec**

建立 `spec/system/transfers_spec.rb`：

```ruby
require "rails_helper"

RSpec.describe "帳戶間轉帳", type: :system do
  let(:user) { create(:user) }
  let(:household) { user.household }
  let!(:account_a) { create(:account, household: household, name: "支票帳戶", starting_balance: 10_000) }
  let!(:account_b) { create(:account, household: household, name: "儲蓄帳戶", starting_balance: 0) }

  before do
    account_a.recalculate_balance!
    account_b.recalculate_balance!
    sign_in(user)
    expect(page).to have_text("Ready to Assign")
  end

  describe "建立轉帳" do
    it "Happy path：從帳戶頁點轉帳，填表後兩帳戶各出現轉帳紀錄且餘額正確" do
      visit account_path(account_a)
      click_link "轉帳"

      expect(page).to have_current_path(new_transfer_path(from_account_id: account_a.id))

      select "儲蓄帳戶", from: "目標帳戶"
      fill_in "金額", with: "3000"
      fill_in "備註（選填）", with: "月存款"
      click_button "確認轉帳"

      expect(page).to have_current_path(account_path(account_a))
      expect(page).to have_text("轉帳已建立")
      expect(page).to have_text("轉出 → 儲蓄帳戶")
      expect(page).to have_text("-NT$3,000")
      expect(page).to have_text("NT$7,000")  # 10000 - 3000

      visit account_path(account_b)
      expect(page).to have_text("轉入 ← 支票帳戶")
      expect(page).to have_text("NT$3,000")  # balance
    end
  end

  describe "刪除轉帳" do
    before do
      # 預先建立一筆轉帳
      visit account_path(account_a)
      click_link "轉帳"
      select "儲蓄帳戶", from: "目標帳戶"
      fill_in "金額", with: "2000"
      click_button "確認轉帳"
    end

    it "刪除轉帳後兩筆同時消失，兩帳戶餘額還原" do
      visit account_path(account_a)
      expect(page).to have_text("轉出 → 儲蓄帳戶")

      accept_confirm do
        find("button[aria-label='刪除交易']", visible: false).click
      end

      expect(page).not_to have_text("轉出 → 儲蓄帳戶")
      expect(page).to have_text("NT$10,000")  # 還原

      visit account_path(account_b)
      expect(page).not_to have_text("轉入 ← 支票帳戶")
      expect(page).to have_text("NT$0")  # 還原
    end
  end

  describe "錯誤情境" do
    it "來源與目標帳戶相同時顯示錯誤訊息" do
      visit new_transfer_path(from_account_id: account_a.id)
      select "支票帳戶", from: "目標帳戶"
      fill_in "金額", with: "1000"
      click_button "確認轉帳"

      expect(page).to have_current_path(transfers_path)
      expect(page).to have_text("來源與目標帳戶不可相同")
    end
  end
end
```

**Step 2: 執行 spec 確認失敗**

```bash
bundle exec rspec spec/system/transfers_spec.rb
```

預期：所有 examples 失敗（controller 還沒實作 create）。

**Step 3: 實作 TransfersController#create**

在 `app/controllers/transfers_controller.rb` 的 `create` action 填入：

```ruby
def create
  from_account = Current.household.accounts.find(params[:from_account_id])
  to_account   = Current.household.accounts.find(params[:to_account_id])
  amount       = params[:amount].to_d
  date         = params[:date]
  memo         = params[:memo].presence

  if from_account.id == to_account.id
    @error = "來源與目標帳戶不可相同"
    @accounts = Current.household.accounts.active.order(:name)
    @from_account_id = params[:from_account_id]
    return render :new, status: :unprocessable_entity
  end

  ActiveRecord::Base.transaction do
    outgoing = from_account.transactions.create!(
      amount: -amount, date: date, memo: memo, category_id: nil
    )
    incoming = to_account.transactions.create!(
      amount: amount, date: date, memo: memo, category_id: nil,
      transfer_pair_id: outgoing.id
    )
    outgoing.update!(transfer_pair_id: incoming.id)
  end

  from_account.recalculate_balance!
  to_account.recalculate_balance!
  redirect_to account_path(from_account), notice: "轉帳已建立"
rescue ActiveRecord::RecordInvalid => e
  @error = e.message
  @accounts = Current.household.accounts.active.order(:name)
  @from_account_id = params[:from_account_id]
  render :new, status: :unprocessable_entity
end
```

**Step 4: 執行 happy path spec 確認通過**

```bash
bundle exec rspec spec/system/transfers_spec.rb -e "Happy path"
```

**Step 5: Commit**

```bash
git add app/controllers/transfers_controller.rb spec/system/transfers_spec.rb
git commit -m "feat: implement TransfersController#create with system spec"
```

---

### Task 6: 交易列表顯示轉帳資訊（_row 更新 + N+1 預載）

**Files:**
- Modify: `app/controllers/accounts_controller.rb`
- Modify: `app/views/transactions/_row.html.erb`

**Step 1: AccountsController#show 加 eager loading**

在 `app/controllers/accounts_controller.rb` 的 `show` action，將：

```ruby
@transactions = @account.transactions.recent.limit(50)
```

改為：

```ruby
@transactions = @account.transactions.includes(transfer_pair: :account).recent.limit(50)
```

**Step 2: 更新 `_row.html.erb`**

將 `app/views/transactions/_row.html.erb` 完整替換為：

```erb
<tr class="border-t border-slate-50 hover:bg-slate-50 group transition-colors" id="transaction-<%= transaction.id %>">
  <td class="px-5 py-3 text-sm text-slate-500"><%= transaction.date.strftime("%m/%d") %></td>
  <td class="px-5 py-3 text-sm text-slate-800"><%= transaction.memo.presence || "─" %></td>
  <td class="px-5 py-3 text-sm text-slate-400">
    <% if transaction.transfer? %>
      <% partner_name = transaction.transfer_pair.account.name %>
      <% if transaction.amount < 0 %>
        轉出 → <%= partner_name %>
      <% else %>
        轉入 ← <%= partner_name %>
      <% end %>
    <% else %>
      <%= transaction.category&.name || "收入" %>
    <% end %>
  </td>
  <td class="px-5 py-3 text-right text-sm font-medium <%= transaction.amount < 0 ? 'text-red-500' : 'text-emerald-600' %>">
    <%= number_to_currency(transaction.amount, unit: "NT$", precision: 0) %>
  </td>
  <td class="px-5 py-3 text-right flex items-center gap-2 justify-end">
    <% unless transaction.transfer? %>
      <%= link_to "編輯", edit_account_transaction_path(account, transaction),
          class: "text-sm text-blue-500 hover:text-blue-700" %>
    <% end %>
    <% if transaction.transfer? %>
      <%= button_to transfer_path(transaction),
            method: :delete,
            data: { turbo_confirm: "確定刪除這筆轉帳？兩個帳戶的對應紀錄都會一併刪除。" },
            class: "opacity-0 group-hover:opacity-100 transition-opacity text-slate-300 hover:text-red-500",
            aria: { label: "刪除交易" } do %>
        <%= icon "trash", classes: "w-4 h-4" %>
      <% end %>
    <% else %>
      <%= button_to account_transaction_path(account, transaction),
            method: :delete,
            data: { turbo_confirm: "確定刪除這筆交易？" },
            class: "opacity-0 group-hover:opacity-100 transition-opacity text-slate-300 hover:text-red-500",
            aria: { label: "刪除交易" } do %>
        <%= icon "trash", classes: "w-4 h-4" %>
      <% end %>
    <% end %>
  </td>
</tr>
```

**Step 3: 手動驗證顯示正確**

```bash
bundle exec rails server
# 建立一筆轉帳後，帳戶列表應顯示：
# 來源帳戶：轉出 → 目標帳戶名稱
# 目標帳戶：轉入 ← 來源帳戶名稱
# 轉帳列不顯示「編輯」連結
```

**Step 4: Commit**

```bash
git add app/controllers/accounts_controller.rb app/views/transactions/_row.html.erb
git commit -m "feat: display transfer label in transaction row, preload transfer_pair"
```

---

### Task 7: TransfersController#destroy 與完整 system spec

**Files:**
- Modify: `app/controllers/transfers_controller.rb`

**Step 1: 實作 destroy action**

在 `app/controllers/transfers_controller.rb` 的 `destroy` action 填入：

```ruby
def destroy
  transaction = Transaction
                  .joins(:account)
                  .where(accounts: { household_id: Current.household.id })
                  .find(params[:id])

  pair         = transaction.transfer_pair
  from_account = transaction.account
  to_account   = pair&.account

  transaction.destroy
  pair&.destroy
  from_account.recalculate_balance!
  to_account&.recalculate_balance!

  redirect_back_or_to accounts_path, notice: "轉帳已刪除"
end
```

**Step 2: 執行完整 system spec**

```bash
bundle exec rspec spec/system/transfers_spec.rb
```

預期：3 個 examples，0 failures。

**Step 3: 執行全部 spec 確認沒有 regression**

```bash
bundle exec rspec
```

預期：0 failures。

**Step 4: Commit**

```bash
git add app/controllers/transfers_controller.rb
git commit -m "feat: implement TransfersController#destroy with linked deletion"
```

---

## 完成確認清單

- [ ] `Transaction#transfer_pair` 正確回傳另一筆
- [ ] `Transaction#income?` 排除轉帳情境
- [ ] `/transfers/new?from_account_id=X` 預選來源帳戶
- [ ] 轉帳建立後，兩帳戶各顯示「轉出 →」/ 「轉入 ←」標籤
- [ ] 轉帳列不顯示「編輯」連結
- [ ] 刪除任一側，兩筆同時消失，兩帳戶餘額同步更新
- [ ] 來源 = 目標帳戶時顯示錯誤訊息
- [ ] 全部 system spec 通過
